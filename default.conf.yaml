- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: default
    ~/.tmux.conf: default/tmux.conf
    ~/.tmux.conf.local: default/tmux.conf.local
    ~/.vimrc: default/vimrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
